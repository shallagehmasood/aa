name: Build Release APK for Alert_X

on:
  push:
    branches: [ "Alert_X" ]  # فقط روی شاخه  Alert_X اجرا شود
  workflow_dispatch:         # امکان اجرای دستی از طریق GitHub UI

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'  # نسخه پایدار فعلی — می‌توانی به نسخه خودت تغییرش بدی
          channel: 'stable'

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Verify Flutter environment
        run: flutter doctor

      - name: Get dependencies
        run: flutter pub get

      - name: Build release APK
        run: flutter build apk --release

      - name: Upload APK as artifact
        uses: actions/upload-artifact@v4
        with:
          name: Alert_X-release-apk
          path: build/app/outputs/flutter-apk/app-release.apk
