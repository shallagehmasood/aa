name: alert_x_app
description: "اپ جایگزین بات اولین هیدن - نسخه Alert_X"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.16.0
  shared_preferences: ^2.2.2
  fluttertoast: ^8.2.4
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
